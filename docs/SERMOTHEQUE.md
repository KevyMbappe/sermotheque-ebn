# Sermothèque EBN — Sermon & Service System of Record

**Status:** Draft v1 (for grilling)
**Last updated:** 2026-08-04
**Owner:** kevy@merca.team
**Relationship to other docs:** This is the **parent project**. The app suite ([PRD.md](PRD.md)) and the website sermon library are *downstream consumers* of this system.

---

## 1. Mission

Build the church's durable **system of record for its preaching** — a clean, rich, portable catalog of every sermon and service, plus the pipeline that keeps it fed.

**Principle:** *The catalog is the asset; every app, site, and platform is a replaceable window onto it.* YouTube, SoundCloud, the website, the future mobile/TV apps — all are rendering surfaces. The content compounds in value for decades; the shells come and go. So we build the content system well, once, and let it outlive everything downstream.

## 2. Architecture — three layers

```
┌─ AUTHORING (write)  — replaceable input surfaces
│   • WordPress admin   (humans: media team, elders — friendly guided forms)
│   • the pipeline      (machines: yt-dlp · ASR · LLM · thumbnail render)
│        │ both write Sermon/Service records
│        ▼
├─ CANONICAL (own)  ★ THE PROJECT ★  — the durable asset
│   • clean, app-agnostic, VERSIONED dataset (structured records + media refs)
│   • a stable Catalog API + full-text / transcript search index
│   • single source of truth; portable; survives WordPress being thrown away
│        │ everything reads from here
│        ▼
└─ CONSUMPTION (read)  — replaceable output surfaces
    • WP website sermon library   • mobile + TV apps   • YouTube thumbnail pusher
```

**WordPress authors, but does not own.** Volunteers keep the friendly admin; every change syncs out to the canonical dataset, which is the real source of truth.

### Canonical storage (recommended)
- **Serving + search:** a real datastore (lightweight DB or prebuilt static JSON index) exposes the **Catalog API** that shells read.
- **Durable archive of record:** a **version-controlled flat-file export** (one JSON record per sermon/service, committed to git) — diff-able, portable, human-readable, survives any platform. Regenerated on every change.
- Church-scale volume (hundreds → low thousands) means static JSON + a prebuilt search index is plenty and near-zero ops; a server DB (Meilisearch/Typesense/Postgres) can replace the serving layer later if scale demands, without touching the canonical archive.

## 3. Canonical data schema (the heart — grill this)

First-class, related entities (not flat tags):

### `Sermon` — the core preaching unit
| Field | Type | Notes |
|---|---|---|
| `id` | slug + UUID | **stable, permanent** — the cross-shell contract (e.g. `2026-03-15-galates-6-14`) |
| `title` | text | |
| `language` | enum | `fr` (default) · `en` · `pt`; conference sermons exist in EN |
| `date` | date | preached date |
| `speaker_id` | → Speaker | |
| `series_id` | → Series (nullable) | |
| `series_part` | int (nullable) | "Partie V" → 5 |
| `primary_scripture` | ScriptureRef | the passage preached |
| `scripture_refs[]` | ScriptureRef[] | other passages cited in the body (from transcript), **as the LLM wrote them** — free French text ("Jean 1:1,14") |
| `scripture_refs_osis[]` | OSIS id[] | ^ the same citations normalised to canonical OSIS (`John.1.1`, `Gen.9-Gen.10`), de-duplicated, order of first appearance. **Derived, not stored** — recomputed by the writeback on every build (#56). The queryable half of the pair; `scripture_refs` stays the display half. |
| `topics[]` | → Topic[] | curated vocabulary |
| `summary` | text | AI-generated, human-confirmed |
| `transcript_ref` · `captions_ref` · `segments_ref` | files | ASR output (labeled "auto-generated"), named **`<entry-id>.<ext>`** — `<id>.txt` (plain) + `<id>.vtt` (segment timing) + `<id>.json` (word timing + confidence), sharing a stem with the catalog `id`. Translated subtitles later use a language infix: `<id>.en.vtt`, `<id>.pt.vtt`. See the naming convention in #42. |
| `media` | object | `{ soundcloud_url, youtube_id, audio_duration }` |
| `thumbnail_ref` | image | from the pipeline |
| `translation_of` | → Sermon (nullable) | links EN/FR versions of the same conference sermon |
| `provenance` | object | per-field: `ai_suggested` vs `human_confirmed` + source + confidence |
| `status` | enum | `draft` · `enriching` · `needs_review` · `published` |
| `created/updated/version` | meta | audit trail (append-only history) |

### `Service` — full Sunday service (light)
| Field | Type | Notes |
|---|---|---|
| `id` | slug + UUID | |
| `date` | date | |
| `title` | text | "Culte Dimanche DD/MM" |
| `youtube_id` | text | full-service / past live stream |
| `duration` | int | ~2 h |
| `preacher_id` | → Speaker (nullable) | |
| `sermon_id` | → Sermon (nullable) | the sermon cut from this service, if identified |
| `streamed` | bool/derived | feeds the app's Live/archive |

### Supporting entities
- **`Series`** — `id`, `name`, `type` (`sermon-series` \| `conference`), `description`, `date_range`, derived `sermon_count`.
- **`Speaker`** — `id`, `name`, `role` (`pastor`/`elder`/`guest`), `is_guest`, optional `bio`, `photo`.
- **`Topic`** — `id`, `label` (FR + EN/PT translations of the *label* only), `description`, derived `sermon_count`. Curated vocabulary, AI-bootstrapped from the corpus.
- **`ScriptureRef`** — structured value object: `book` (canonical name), `chapter`, `verse_start`, `verse_end`. Enables **browse-by-book** ("all sermons in Romans"). *(Open: adopt a standard like OSIS for book IDs.)*

### Provenance & "confirm-don't-type"
Every auto-extracted field carries provenance (`ai_suggested` / `human_confirmed`, source, confidence). This powers the **completeness dashboard** (§5) — the system always knows what still needs a human eye.

## 4. The pipeline (per sermon — new + backfill)

1. **Ingest + parse title** — SoundCloud track (spine) + matched YouTube video. Parse the structured SC title `Title | Scripture | (Pr. Speaker)` → pre-fill title/scripture/speaker/series (~74% yield, free).
2. **Transcribe** — ASR (Whisper large-v3-class) on the **clean SoundCloud audio** → transcript + `.vtt`. Clean source = no hymn/offering pollution.
3. **AI enrich** — LLM reads the transcript → proposes `topics` (from vocabulary), `summary`, confirms/augments scripture refs.
4. **Human pass** — one screen: confirm AI suggestions **and** pick the thumbnail frame (top 3). Confirm-don't-type.
5. **Render thumbnail** — branded overlay over chosen frame → 1280×720.
6. **Publish + export** — write record; regenerate canonical export + search index; push thumbnail to YouTube (Data API); fire notification.

## 5. Management surface

- **Authoring:** locked-down WP admin guided form (publish new) — for the media team / elders.
- **Curation console (the "management solution"):** a catalog-wide view with a **completeness dashboard** — surfaces what's missing or unconfirmed: no scripture, unconfirmed AI fields, unmatched YT↔SC, no thumbnail, no transcript, orphan series. Bulk edit; manage Series / Speakers / Topics. Recommended to live as custom pages **inside WP admin** (one tool, same records), not a second app.
- **Roles:** Publisher (media team/elders) + Admin (pastor + technical owner) — per [PRD.md](PRD.md) §8.

## 6. Backfill plan (measured inventory — raw files in `data/raw/`)

| Source | Count | Role |
|---|---|---|
| SoundCloud tracks | **239** | **spine** of the sermon catalog (cleanest titles) |
| YouTube Videos | **300** | cut-sermon videos — matched in by title/date |
| YouTube Live | **102** | `Service` records (light) |

Sequence:
1. **Instant first-pass catalog** — parse the 239 SC titles → structured Sermon records (title/scripture/speaker/series). A usable catalog *immediately*, no ASR needed.
2. **Match YT → SC** — fuzzy title + date match the 300 videos onto Sermons; attach `youtube_id`; report orphans (likely EN conference videos with no SC counterpart).
3. **Import Services** — the 102 streams → `Service` records.
4. **Enrich** — batch ASR + LLM over the corpus → transcripts, topics, summaries, search index.
5. **Human confirmation sweep** — driven by the completeness dashboard.
6. **Thumbnails** — pipeline over the catalog.

## 7. Roadmap

- **M1 — Foundations & instant catalog:** canonical schema + repo + export format; WP authoring model; first-pass backfill from SC titles (a real catalog on day one).
- **M2 — Matching & console:** YT↔SC matching + services import; completeness dashboard.
- **M3 — Enrichment & search:** ASR + LLM + full-text/transcript search.
- **M4 — Thumbnails:** branded thumbnail pipeline + YouTube push.
- **M5 — First public window:** website sermon-library section reading the Catalog API.
- **Later — Apps:** mobile + TV consume the same Catalog API ([PRD.md](PRD.md)).

## 7b. Build log

> Append a dated entry here whenever you complete a step, and follow the **Maintenance protocol** in `CLAUDE.md` (sync README/CLAUDE.md status + roadmap, commit, push).

**M1 first-pass catalog — DONE (2026-06-13).** `pipeline/parse_catalog.py` parses the 239 SoundCloud titles → `data/catalog/catalog.json` + `.csv`. Coverage (titles only, no ASR):
- Scripture (OSIS): **196/239 (82%)**, 80% high-confidence, across **24 books**.
- Clean title: 87% · Speaker: 26 named (mostly the regular pastor is untagged) · Series part: 34 · English: 0 (SoundCloud is FR-only).
- Distribution reveals systematic expository series: **Galates 72, Hébreux 28, Genèse 16, Jacques 14, Ézéchiel 11**.
- Known limitations carried to enrichment: the 43 refless items (intros/Q&A/topical), speaker inference for the regular preacher, multi-range verses approximated as a span.

**M1b series clustering — DONE (2026-06-13).** `pipeline/cluster_series.py` → `data/catalog/series.json` + enriches catalog. **22 series, 81% of sermons placed**, ordered by chapter:verse. Expository: Épître aux Galates (69), aux Hébreux (28), de Jacques (14), Genèse (13), Ézéchiel (11)… Thematic: Joie chrétienne, Noël le plus glorieux des mystères, Fruit de l'Esprit… Pipeline order: `pipeline/build.py` runs parse → cluster → match.

**Git — DONE (2026-06-13).** Repo initialized (branch `main`); the canonical dataset is now version-controlled per the durability decision. Initial commit `e2d3218`. Pushed to private GitHub repo `KevyMbappe/sermotheque-ebn`.

**M2 YouTube↔SoundCloud matching — DONE (2026-06-13).** `pipeline/match_youtube.py` (scripture-anchored + language-aware) → enriches catalog + `data/catalog/youtube_orphans.json`. **Finding: YouTube's Videos tab is NOT a mirror of SoundCloud** — only **17** confident same-language overlaps. YT is dominated by **conference content (48, CBN Paris / international guests)**, **English** material, and ~215 French videos not title-matchable to SC. The real catalog is the **UNION**, not the 239 SC spine. *Decision #37 (below).*

**M2b duration fingerprint — DONE (2026-06-13).** Re-pulled SoundCloud **durations + dates** for all **239/239** tracks (fast extraction rate-limits after ~146; a throttled `--sleep-requests 1.5` pass completed the rest). Data now `id⇥duration⇥upload_date⇥title`. Calibrated on known-same pairs: **YouTube runs a near-constant +51 s longer than SoundCloud** (range +26…+63 — an intro/outro card) — a sermon's length is effectively a fingerprint. Added an offset-corrected duration signal to the matcher. **Final result (full coverage):** same-language matches **17 → 61**, translations **2 → 11**, orphans **281 → 228**. **Union = 467 distinct sermons** (239 SC + 228 YT-only: conference ~45, English ~21, SC-absent French ~162). No caveat — duration coverage is complete.

**M3 ASR + LLM enrichment spike — DONE (2026-06-13).** Transcribed one sermon (mlx-whisper `large-v3-turbo`, ~6× real-time, local/free) and enriched it. **PASS** — excellent French ASR (cosmetic artifacts only), accurate zero-hallucination topics/summary, and it *recovered series context the title lacked* (Confession de foi 1689, ch. 8). Refined division of labor: **title → primary scripture; transcript → topics/summary/series/search**. Speaker inference is the weak spot (use a default rule, not ASR). Full write-up: `docs/research/METHODOLOGY.md`.

**M3b ASR bigger sample (n=8, 2023→2026) — DONE (2026-06-13).** 8 sermons across the archive (incl. oldest + multiple speakers): **zero failures, uniformly high quality across all eras**. Errors are **systematic** (foi→fois, le→les, Dieu→dieux for the regular pastor) → cheap regex post-clean. Different (native-FR) speakers transcribe even cleaner (artifact = weak speaker fingerprint). **Speed (measured on a full sermon):** ~15× real-time ⇒ 239 SC sermons ≈ **~15 h local compute** (a couple of overnight runs). Verdict: green-light the full pass, with a regex normalization step + a default-speaker rule.

**M4 fold orphans → unified catalog — DONE (2026-06-13).** `pipeline/fold_orphans.py` merges the 239 SoundCloud records + 228 YouTube orphans into **one catalog of 467 records** with a uniform canonical schema (`id`, `source`, `media`, enrichment placeholders). Pipeline order is now **parse → match → fold → cluster** (`build.py`); series clustering runs over the union → **25 series** (new ones surfaced from YT/conference content). `catalog.csv` is now the flattened 467-row export written by the final step. *(Not yet folded: the 102 Live `Service` records — a separate light type.)*

**M5 enrichment pipeline — built (2026-06-13).** `pipeline/{transcribe,enrich,build_entry}.py` — single entry point `build_entry(source, *, transcribe, enrich)` (YT/SC ids/URLs → one canonical catalog entry). Transcribe + enrich are injected (real adapters: mlx-whisper, Claude API). Conservative ASR cleanup (`clean_transcript`). Tests: `tests/` (stdlib unittest) — **20 tests, offline/deterministic**, covering parsing edge cases, cleanup (with negatives), and end-to-end assembly. Storage per #38 (transcripts→git, audio→gitignored `cache/`).

**M5b POC — DONE (2026-06-14).** Ran the real pipeline on the 8 sample sermons (full audio, 7–11.5k words each): scripture/kind parsed correctly (incl. cross-chapter `Gen.1.1-Gen.2.3`, `Leçon`→teaching). **Real enrichment ran** (`enrich.py` → Claude `sonnet-4-6`, ~16¢, first execution of that path — validated): full-text summaries + body-cited `scripture_refs` (3–15/sermon, vs ~0 in the slice spike) + series context. 8 transcripts committed to `data/catalog/transcripts/`; demo entries `docs/research/poc_entries.json`; elder-facing `docs/research/POC.{md,pdf}` (5 pp). Environment is now project-local (`.venv` + `requirements.txt`, `cache/`, `.env`/`.env.example`) — no `/tmp`.

**M5c cost reconciliation + model bake-off — DONE (2026-06-14).** The 8-sermon POC enrichment cost **$0.51 ⇒ ~$0.06/sermon on Sonnet 4.6**, *3× the "~$0.02" carried in the docs (that was Haiku pricing) — corrected everywhere.* Ran a **Haiku 4.5 vs Sonnet 4.6** head-to-head on one rich sermon (`sc-2338530545`): both excellent and well-cited; **Sonnet kept as default** — it names heresies precisely and writes clean French where Haiku slipped typos/anglicisms. Full bake-off + pricing reference: [`docs/ENRICHMENT-MODEL.md`](ENRICHMENT-MODEL.md); reasoning in decision #40. Full-pass budget: **~$12 (239 SC) / ~$23 (467 union)**. Also locked **decision #41 (ASR backend pluggable — mlx now, cloud Whisper API on the church's Windows machines in production)**.

**M5d timestamp capture — DONE (2026-06-14, decision #42).** Extended the ASR step to emit **segment + word-level timestamps** in the same pass (mlx-whisper `--output-format all --word-timestamps True`, free). `transcribe_fn` now returns `{text, vtt, segments, language}`; `build_entry` persists `<id>.txt` + `<id>.vtt` (subtitle-ready) + `<id>.json` (word timing + per-segment confidence). Back-compatible — fakes returning a plain string still work. Tests: **20 → 22** (added the rich-result persistence path + the string fallback), all green offline. Unlocks EN/PT subtitles, deep-linking, search-to-moment, and confidence-flagged review without ever re-running ASR. **Transcript naming convention formalized** (see #42): canonical files are `<entry-id>[.<lang>].<ext>`, flat, keyed to the catalog `id`. *(Note: the 8 committed POC transcripts are still text-only `.txt` — they predate this change; they'll gain `.vtt`/`.json` when the full pass runs, since regenerating 8 means a fresh download+ASR. Git-weight: VTT ≈ 2× the `.txt`; word-level JSON is heavier (~5–10×) — committed by default per "capture once", revisit if the union's transcripts dir grows past a comfortable size.)*

**M5m voiceprint capture adapter — DONE (2026-06-16, decision #46).** Wired the speaker embedding into the one-go pass as the **third injected adapter** (`embed.py`, Resemblyzer), beside transcribe/enrich. `make_transcriber(embed=…)` computes a 256-float voiceprint from the **same download** (before the audio is deleted), failing soft so an embed error never costs the transcript; `build_entry` persists it to the id-keyed **voiceprint store** (`voiceprint_store.py` → `data/catalog/voiceprints.json`). On by default in `run_enrichment` / the CLI (`--no-voiceprint` to skip). **Scope is capture-only** — the voiceprint is the *one-shot* audio datum, so it must ride the pass; *attribution* (cosine vs per-speaker centroids → name + the `audio-fingerprint`/`human` provenance rungs) is re-runnable from the stored vectors and is built later. Seeded 20 voiceprints from the embeddings already computed in the M5l test (no re-download). Tests 50 → 53. Tonight's sample run captures voiceprints + validates the wiring live before the full pass.

**M5q series-prior speaker ground truth — DONE (2026-06-17, decision #50).** Broke the elder data-starvation with two church-domain priors (Galates→David, Ézéchiel→Stephan, Jacques→Nathanaël), voiceprint-verified by within-series cohesion. New cheap `capture_voiceprints.py` (download→embed→delete, no ASR/API) filled the data to **117 voiceprints**; priors held (Ézéchiel 11/11, Jacques 14/14, Galates 20/21 + 1 flagged). Centroids: David 2→40, Stephan→19, Nathanaël→16. **55 rows audio-fingerprint** (David 30, Stephan 13, Nathanaël 12); 25 of the 117-sample audio-confirmed not-David. Tests 73 → 79.

**M5t freeze canonical record contract — DONE (2026-06-17, decision #53).** `pipeline/schema.py`: the catalog row shape is now an explicit frozen contract (40 fields, validated; formal `data/catalog/sermon.schema.json` for WP import). Drift guard asserts the two pipeline field-lists exactly cover the schema; `build.py` validates as its final step. Caught `youtube_match` being a number not an object. **Sonnet-only locked** for the full pass (Haiku rejected on quality). Tests 79 → 88.

**M5s decouple capture from enrich + rebuild integrity — DONE (2026-06-17, decision #52).** Readiness work for the catalog-wide pass, matching the audio-once/text-rerunnable split. (1) **`run_enrichment --no-enrich`** = capture-only pass: transcribe + voiceprint, NO Claude call — banks the one-shot audio artifacts (transcript/VTT/voiceprint) at **$0 API**, so the irreversible thermal pass is decoupled from the budget-bound enrichment. (2) **transcript-cache-aware transcriber** (`make_cached_transcriber`): a sermon with a committed transcript is read from disk instead of re-ASR'd — so a later enrich pass reads banked transcripts for free and re-running enrichment never re-heats ASR. Capture-mode "done" = transcript on disk; enrich-mode "done" = in the enrichment store. (3) **`speaker_id.writeback` wired into `build.py`** (was missing) — a structural rebuild now preserves the 100 `audio-fingerprint` labels instead of reverting them to default-rule (same class as #44; verified by a full rebuild). Word-level timestamps confirmed **out** (VTT/subtitles suffice — #42 stands). Tests 79.

**M5p audio speaker attribution — DONE (2026-06-17, decision #49).** Wired `pipeline/speaker_id.py`: centroids from title-confirmed voiceprints, cosine nearest-centroid classification, ladder-aware writeback (`speakers.json` → catalog). LOO 10/10; same/cross-speaker cosine cleanly separated (≥0.94 vs ≤0.89), accept floor 0.90. **15 rows upgraded to `audio-fingerprint`** — incl. the Ézéchiel/Leçon sermons correctly reassigned to **Stephan Kongo** (the audio caught the default-rule mislabels independently). David's 2-example centroid is the bottleneck (many real David sermons score below floor) → bootstrap with `speaker_overrides.json`. Provenance now: 45 title / 15 audio-fingerprint / 413 default-rule / 44 blank. Tests 63 → 73.

**M5o language from audio + retry-on-garbage — DONE (2026-06-17, decision #48).** Fixed a root-cause caveat surfaced by #47(a): ASR hard-coded `--language fr` turned genuinely-non-French audio into `...` garbage + title-hallucinated enrichment, and the title-derived `language` label was unreliable (English titles on French sermons). Now: default forced-fr (no regression to the 489) → **retry forced-en if the transcript looks garbage** → label from **transcript content** (`detect_language`), stored so it overrides the title guess. Also fixed mlx-whisper's stale-output skip (clear sidecars before each ASR pass — the retry was silently re-reading the fr garbage) + YouTube download **retry/back-off** (the #47 403s) + enrichment prompt keeps `anchor`/`key_quotes` in the transcript's language. Re-ASR'd only `yt-HOjW63LMfhQ`; relabeled the 2 French-with-English-title sermons. Tests 56 → 63.

**M5n first substantial enrichment batch (30-sermon checkpoint) — DONE (2026-06-17, decision #47).** Ran a **hand-picked spread of 30 sermons** (18 SC / 12 YT · 27 FR / 3 EN · 10 series / 20 standalone · edge cases: 89-min Paul Washer talk, 2-min conference clip, 75-min English talk, teaching + Q&A kinds, 2 Ézéchiel→Stephan) via `run_enrichment.py --ids` → store **9 → 39 enriched (~7.5% of 517)**, **voiceprints 20 → 46** (first live exercise of the M5m capture wiring — validated end-to-end). **Measured cost: $0.0705/sermon (range $0.013 short clip → $0.098 scripture-dense) ⇒ ~$36 for all 517** — slightly *under* the M5l ~$0.078 estimate. **Robustness findings:** (1) **SoundCloud flawless** (18/18 first try); **YouTube flaky** — intermittent `HTTP 403` throttling hit 3 videos, *all succeeded on retry* → the 500-pass YT path needs built-in retry/back-off, the SC spine doesn't. (2) Repeated harness reaping of the long background runs proved **resumability works perfectly** — each kill cost only the one in-flight sermon, zero wasted compute. **Two text-layer quality findings (decision #47, neither blocks the 500 — both re-runnable from committed transcript+VTT, no re-ASR):** (a) **English sermons break timestamp-anchoring** — French enrichment anchors don't match the English VTT, so EN chapters come back `t:None` with 0 key_quotes/0 scripture_refs (28 EN records affected); (b) **short clips fabricate structure** — the 2-min clip padded to 6 chapters/6 key_points (schema fills to requested counts instead of degrading). Normal sermons excellent (Jacques: 30 scripture_refs, anchored chapters + verbatim quotes; the 89-min Washer talk: 696-char summary, 7 chapters, 11 refs).

**M5l enrichment schema expansion + audio fingerprint validated — DONE (2026-06-16).** Audio voiceprints (Resemblyzer, torch already present) tested on 20 labeled sermons: **8/8 on title-confirmed speakers** (Stephan 6/6, Nathanaël 2/2 — text got Nathanaël 0/2), and it **caught a real default-rule mislabel** — the Ézéchiel sermon `sc-1900264623` (auto-credited David) is **Stephan**, human-confirmed. So Tier B (audio) is the substrate for the speaker store; text Tier A stays a weak flag. Enrichment schema expanded to the full WP-facing set (decision #46 fields): **description, invitation** (warm-sober blogger blurb), **summary, key_points, chapters** (title + verbatim anchor → VTT timestamp), **topics** (broad→specific — an initial separate `doctrines` field was tried then **merged back into topics**, too redundant), **references, key_quotes** (verbatim → VTT timestamp), **questions**, scripture. `build_entry._vtt_locate` anchors quotes/chapters to real times. **`speaker_provenance`** now on every record + in the CSV (428 default-rule / 45 title / 44 blank) — the rung for `audio-fingerprint`/`human` lands when the speaker store is wired. Live validation caught + fixed two run-killer bugs (prompt brace, `max_tokens` truncation). **Cost with the rich schema: ~$0.078/sermon** (Sonnet) ⇒ ~$19/239 SC, ~$40/517. Translation-friendly: French source now, controlled vocabs translate once, prose later — text-derived fields re-runnable from committed transcripts (no re-ASR). Tests 34 → 50.

**M5j production runner + default-speaker rule — DONE (2026-06-16, decision #45).** `pipeline/run_enrichment.py`: the catalog-wide pass over `build_entry` — **resumable** (skips ids already in the store, so a kill at #300 resumes there), **logfile** (`logs/enrichment.log`, per-sermon id/time/tokens/cost + running total), live cost, `--source`/`--ids`/`--limit`/`--model`/`--segments`, and `save_entry` per sermon + a final writeback. Resolves URLs (YT from id; SC via a cached channel map). Smoke-tested on the no-op/resume path. **Default-speaker rule (#45):** untagged sermons → David Pelosi (the regular preacher); conferences/English left unattributed; attribution went 45 → 473/517. Tests 28 → 34. **The full pass is now fully unblocked** — `./.venv/bin/python pipeline/run_enrichment.py --source soundcloud` runs the 239 SC spine (~16 h, ~$15).

**M5i enrichment writeback layer — DONE (2026-06-16, decision #44).** `pipeline/enrichment_store.py`: id-keyed store `data/catalog/enrichment.json` + `writeback()` wired as the final step of `build.py`. Enrichment now lives outside the structurally-regenerated catalog and is re-merged after every rebuild — proven by rebuilding from raw and seeing the 9 seeded entries (8 samples + `yt-IqNmh_XGULE`) come back enriched. Unblocks the full pass (the runner calls `save_entry` per sermon; `build.py` re-applies). Tests 25 → 28.

**M5f pipeline hardening + first real YT run — DONE (2026-06-14).** Ran the full per-sermon pipeline end-to-end on a sermon *outside* the sample-8 — YouTube `IqNmh_XGULE` (*Prier selon le cœur de Dieu IV | Psaume 40*, 59 min): parse/scripture/series-part all correct, 8.4k-word transcript, rich enrichment. Fixes applied off the back of it: (1) **enrichment input cap 48k → 120k chars** — the 48k cap had silently truncated this sermon's conclusion (re-enriching the full text recovered refs *Psaume 51* and *Hébreux 5:7-9*); (2) **YouTube `upload_date` now captured** into the entry (`build_entry` date falls back to `source.upload_date`; the CLI probes it via yt-dlp) — fixes `date: null`; (3) **live progress tracking + cost line** — Tier 0 (yt-dlp/whisper progress streamed to stderr) + Tier 1 (`on_progress(phase)` callback + `on_usage` → `cost_of(model, in, out)` from the real pricing table in `enrich.py`); stdout stays clean JSON; (4) **word-level `.json` now opt-in** (`--segments`), default keeps `.txt`+`.vtt` (decision #42 git-weight). Measured enrich cost on this sermon: **$0.058** (17.4k in + 382 out, Sonnet 4.6). Tests **22 → 25**. Still open: default-speaker rule (`speaker: null`), and full-pass essentials (logfile + resumability).

**M5g catalog correction → M5h matcher hardening — DONE (2026-06-14, decisions #43/#44).** The real run exposed a **false-positive YT↔SC match** (`IqNmh_XGULE` Psaume 40 wrongly attached to the Genèse SC sermon via a 5-second duration collision, 0.656). An audit found the whole **0.6–0.7 band (36 matches) was duration-only and mostly spurious**. Fixed properly in `match_youtube.py` — **duration now only corroborates; title or scripture must carry the match** — and rebuilt: **overlaps 61→18, translations 11→4, orphans 228→278, union 467→517**, with 0 false positives remaining and 0 real matches wrongly demoted (the one-off 468 hand-patch is superseded). The rebuild also surfaced **#44**: enrichment written into `catalog.json` is wiped by a rebuild → the full pass needs a separate id-keyed enrichment store + writeback step (build before scaling).

**M5e docs restructure — DONE (2026-06-14).** `docs/spike-asr/` was a misnomer — it held a live reference doc, an elder-facing POC, *and* genuine spike evidence under one "throwaway" name. Reorganized: **promoted `ENRICHMENT-MODEL.md` → `docs/`** (it's a current reference, consulted before the full pass), **renamed `spike-asr/` → `research/`** (honest: holds the M3 spike + the M5b POC as the historical record), renamed its `transcripts/` → `spike-transcripts/`, and **dropped one duplicate** (`01_…_FULL.txt` was byte-for-byte the canonical `sc-2338530545.txt`). The 8 original spike slices are kept as evidence (they're the literal inputs `METHODOLOGY`/`RESULTS` describe). All ~7 cross-references in `CLAUDE.md`/`README.md`/this spec updated; `git mv` preserved history.

**M5u capture batch — PARTIAL, PAUSED (2026-06-20).** First real execution of the capture-only pass (#52) beyond the hand-picked checkpoint: `run_enrichment.py --no-enrich` over the catalog. **Banked: 139 transcripts (`.txt` + `.vtt`, 130 `.raw.txt`) and 217 voiceprints**, up from 39/46 at the M5n checkpoint — $0 of API spend, as designed. Coverage by source: transcripts **126 SC / 13 YT**, voiceprints **185 SC / 32 YT**. **The pass is unfinished — it stopped in flight at ~27% of the 517** and has not been resumed since. **Integrity gap to check before resuming:** **79 ids hold a voiceprint but no committed transcript** (59 SC / 20 YT), although `build_entry` writes both from the same download. Either the runs were killed between the two writes, or those transcripts were never committed. Resolve first — resumability keys capture-mode "done" off *the transcript on disk*, so those 79 will be re-downloaded and re-ASR'd (correct but wasteful) while their voiceprints are already banked.

**M5v enrich-from-disk batch — DONE (2026-06-22).** Enriched the banked transcripts with no re-ASR, exercising the cache-aware transcriber from #52: store **39 → 139 enriched (~27% of 517)**, 126 SC / 13 YT, on `claude-sonnet-4-6` per #53. Confirms the decoupling works end to end — the text layer advanced by 100 sermons without touching audio, downloads, or the thermal budget. Speaker attribution after writeback: **100 `audio-fingerprint` · 45 `title` · 331 `default-rule` · 41 blank** (`speakers.json`, 127 classified rows). Catalog totals unchanged (517 rows, 26 series, schema-valid, 88 tests green).

**M6a POC web — WIP (2026-07-30, decision #54).** First **consumption-layer** artifact: `apps/web-poc/`, a React 18 + Vite 6 static app that reads the canonical catalog directly. `scripts/build-data.mjs` runs before every dev/build and projects `data/catalog/` into `public/data/` (enriched rows only, + per-sermon VTT copies), so the site can never drift from the committed dataset — the architecture's "apps are windows" rule, enforced by the build. Pages Home / Browse / Sermon; components Player (SoundCloud Widget + YouTube IFrame), Transcript (VTT-synced, `lib/vtt.js` is a JS port of `build_entry._vtt_cues` so the cues line up with the enrichment anchors), Chapters (deep-link into the audio), Filters, SermonCard. **Publishes 131 of the 139 enriched rows** — the 8 M5b sermons predate the `description` field and need a ~$0.55 re-enrich to appear. Verified on screen: home page, filters, cards. **Not yet verified: the sermon page end-to-end** (player seek, synced transcript) and the entire YouTube IFrame path; no deploy workflow written. Plan + resume notes: [`docs/plans/WEB-POC.md`](plans/WEB-POC.md) and [`WEB-POC-STATUS.md`](plans/WEB-POC-STATUS.md).

**M6b POC web — mobile vérifié + déploiement câblé (2026-08-01).** First actual `npm run build` of the POC (it had never been run: 38 modules, 164 KB JS / 53 KB gzipped, 8.8 KB CSS) and first look at the **sermon page on screen** — it renders fully (header, invitation, embedded player, summary, key points, chapters, transcript). **Responsiveness measured, not eyeballed**, via Playwright device emulation at 320/360/375/390/414/768 px, comparing `scrollWidth` to `clientWidth`. Found and fixed **three real horizontal-overflow defects**: (1) `.site-header` was `display:flex` with no `flex-wrap`, so the nav pushed the page 1 px past the viewport at 390 px and 71 px at 320 px; (2) `.filters` grid children kept `min-width:auto`, so the search input's intrinsic width (~312 px) overflowed below ~340 px; (3) `.grid` used `minmax(280px, 1fr)`, which forces overflow in any container narrower than 280 px — now `minmax(min(280px, 100%), 1fr)`. **All six widths are now clean.** Verdict recorded honestly: the app is **fluid and now mobile-correct, but not authored mobile-first** (desktop-out base styles + one `max-width: 820px` breakpoint, rather than small-screen base + `min-width` queries). **The chapter→player seek — the interaction the whole pipeline exists for — is confirmed working on desktop** (verified by the author outside the container; the sandbox blocks SoundCloud/YouTube, so the SDKs never load here and the graceful degradation fired instead, which is itself the first live proof of that fallback). Still unexercised: the **YouTube IFrame** path (distinct code) and an EN sermon. **Speaker honesty fix shipped with it:** 72 of the 131 published rows credit David Pelosi by *default rule* — an inference, previously rendered identically to a confirmed attribution. Default-rule rows now carry a discreet unconfirmed marker (`?` on cards, "attribution par défaut, à confirmer" on the sermon page), distinct from the `♪` that means audio-verified. Publishing a real preacher's name on a guess, unmarked, is not acceptable for a church's record. Deployment wired: `.github/workflows/deploy-poc.yml` (see #54/#55).

**M6c POC web EN LIGNE — DONE (2026-08-01).** First public deployment of anything in this project: **https://kevymbappe.github.io/sermotheque-ebn/**, serving **131 enriched sermons** with player, timestamped chapters and synced transcript. Run #3 green end to end (build 20 s, `deploy-pages@v4` at 12:38:03). Took three runs, and the two failures are worth keeping: (1) `enablement: true` — an action parameter that cannot work, because creating a Pages site needs repo-admin rights a workflow's `GITHUB_TOKEN` does not have; (2) after removing it, the site still had to be switched on **by a human, once** — Settings → Pages → Source = *GitHub Actions*. Nothing in the build was ever wrong: `npm ci`, the catalog projection and the Vite build passed on the very first attempt and every attempt since. **What this proves beyond the demo:** the whole chain *canonical dataset → build-time projection → public site* runs unattended, so a future enrichment pass republishes the site by itself (the workflow watches `data/catalog/**`). The catalog is no longer only an archive — it feeds a live surface.

**M5w in-body citations normalised to OSIS — DONE (2026-08-01, decision #56).** The enrichment already extracted the passages *quoted inside* each sermon (`scripture_refs`), but only as free French text, so **1 243 citations across 139 enriched sermons were dead weight** — displayable, not queryable. `scripture.parse_reference` / `normalize_refs` (new, in the shared primitives, reusing the existing `BOOKS` table) turn each string into OSIS ids, and `enrichment_store` derives **`scripture_refs_osis`** on every writeback. **Measured on the committed dataset: 1 243/1 243 parsed, 0 failures**, 1 248 OSIS ids emitted (a few strings expand to several: verse lists, several books in one string), 923 distinct, **380 distinct book+chapter keys**. Verified beyond "non-empty": an independent check confirms **no number is silently dropped** from any of the 940 distinct strings, and every emitted id is well-formed with a book from `BOOKS`. **Reach: 54 distinct Bible books are now addressable through in-body citations, against 22 via `scripture_book` on the same 139 rows** (28 across all 517) — 27 books, among them Lévitique, Josué, Job, Esther, Néhémie, Daniel, Osée, Michée, Malachie and Jude, become findable for the first time. Handled honestly rather than guessed: chapter ranges (`Genèse 9-10` → `Gen.9-Gen.10`, *not* `Gen.9.10`), verse lists (`Jean 1:1,14` → two ids), cross-chapter spans, dot separators (`Josué 1.8`), French `ch./v./à`, single-chapter books (`Jude 24-25` → `Jude.1.24-Jude.1.25`), parenthetical glosses stripped (`Luc 15 (fils prodigue)`) unless the parenthesis is itself a citation, and book-only allusions kept as book-level ids (40 occurrences, 21 distinct). Anything with no recognisable book returns `[]` and is counted, never invented. Tests **88 → 123**. Next step this unlocks: the inverted index (`Rom.8` → the sermons that cite it) and browse-by-passage.

**M6e browse-by-passage — DONE (2026-08-01).** The inverted index #56 left "deliberately out of scope" is now built, in the consumption layer where it belongs. `apps/web-poc/src/lib/passages.js` turns the catalog into `book → chapter → sermons`, computed **client-side** (~1 250 citations today, ~4 600 once all 517 are enriched — instant, and it keeps a single source of truth instead of a second file to hold in sync). It distinguishes two relations that are not worth the same to a reader: **preached** (the passage *is* the message's text, `scripture_osis`) and **cited** (convoked inside it, `scripture_refs_osis`); the stronger one wins when both apply. Chapter ranges are expanded, so `Genèse 9-10` is findable under Genesis 10 and not only 9. Surfaces: **`/livres` becomes a real front door** — 54 book tiles in canon order with both counts, against a plain group-by on `scripture_book` which showed only the 22 preached ones; **`/livres/:book`** lists what was preached on it, then a chapter-by-chapter index; and on a sermon page the **cited passages became links**. Those chips are built from the OSIS ids, *not* by pairing them index-by-index with the free-text list — the two lists diverge on 2 of 134 rows (`scripture_refs_osis` is de-duplicated and one string can yield several ids), which would have produced a link that is right 98 times out of 100 and therefore wrong on a real page; the verbatim French references are kept underneath, where verse-level precision survives. The 54 book pages are pre-rendered too ("prédication sur Malachie" is exactly what someone types into a search engine). `src/lib/books.js` was split out of `data.js` so the build script shares the same book table instead of copying it. Verified: 54 tiles, Galates 22 preached / 32 citing, Malachie–Jude–Esther reachable for the first time, chip → book page navigation, 0 JS errors, layout still clean 320→768 px.

**M6f curated topic vocabulary + browse-by-theme — DONE (2026-08-01, decision #57).** Topic navigation was impossible: 594 distinct free-text labels for 139 sermons, 451 of them appearing once. `pipeline/topics.py` adds a **44-category curated vocabulary** and derives `topics_canonical` on every writeback — **98% of the 1 086 label occurrences classified**, 44/44 categories used, the remaining 14 being book names, excluded on purpose. `build.py` gained a step that refreshes `data/catalog/topics.json` (vocabulary + counts + the unmapped worklist) so the church can extend it without touching code. On the site: **`/themes` lists the 44 themes with their counts**, `/themes/:id` lists the sermons (Sanctification 51, L'Église 40, Péché et chute 39…), the topic chips on a sermon page became links, and a topic filter joined the home page. All 44 theme pages are pre-rendered like the book pages. Verified: 44 tiles, chip → theme page, 0 JS errors, layout still clean 320→768 px. Tests 123 → 153.

**M6g POC polish — full-text search, printable sheet, preview cards, Xcode scaffold removed — DONE (2026-08-01).** Four things. **(1) Full-text search** (`src/lib/search.js`): the old one was an `includes` over five fields ordered by date, so a word living in a quote, a reflection question or a reference was unfindable, and nothing said *why* a sermon surfaced. Now every editorial field is indexed, **fields are weighted** (title 10 → question 1) so results rank by relevance, and each card shows **the field that matched plus a highlighted excerpt** — rendered as JSX segments, never as injected HTML. `fold` moved to its own module: `data.js` → `search.js` → `data.js` was an import cycle that works only as long as usage stays deferred. **(2) Printable home-group sheet:** a button plus `@media print` — player, transcript, navigation and share buttons drop out; title, passage, summary, key points, quotes and **the reflection questions, spaced to write under**, remain, under a paper header carrying the church name and the sermon URL. No new component: the material was already generated, only the paper layout was missing. **(3) Preview cards** (`scripts/og-image.mjs`): one 1200×630 SVG per sermon rasterised to PNG, so a shared link finally carries an image; pages move to `summary_large_image`. The rasteriser sits behind an `available()` probe — a missing native binary degrades to no image instead of failing the deploy. The character-width factor was **calibrated against the rendered PNG** (0.52 overflowed the frame; 0.62 fits) — the kind of error only visible by looking at the output. **(4)** The stray Xcode SwiftUI scaffold flagged in PRD #38 is **deleted**; PRD #9 (Expo) stands unchallenged. **Data finding:** **5 published sermons carry an empty `title`** — their raw title was *only* a scripture reference, which the parser strips by construction. The web projection falls back to the passage, but this needs fixing upstream: a record with no title will hurt the WordPress import too.

**M6h fiche sermon restructurée — DONE (2026-08-02).** Deux retours d'usage disaient la même chose : « trop de blocs de texte, ça fait peur » et « la transcription est trop loin du lecteur ». La page affichait **tout** ce que le pipeline produit, empilé dans l'ordre où c'était commode à rendre — un inventaire, pas une hiérarchie — et mélangeait deux usages qui se disputaient l'écran : ÉCOUTER (lecteur, chapitres, transcription) et ÉTUDIER (résumé, points clés, citations, questions). Ordre revu (lecteur → chapitres → transcription → résumé → le reste), **tout replié sauf le résumé** (`components/Section.jsx` : titre + compteur, contenu conservé dans le DOM donc trouvable au Ctrl+F, et rouvert à l'impression puisque la fiche de groupe doit rester complète), invitation déplacée de l'en-tête vers le résumé — **le lecteur commence désormais à 414 px au lieu de ~1 250 px sur mobile**, donc visible sans défiler. **Le bloc lecteur est collant** (`.player-dock`) : on garde le lecteur SoundCloud d'origine, donc sa **barre de progression native** — reculer précisément était justement le geste visé, et une commande « −10 s » maison imposait un pas fixe pour plus de code. Il se colle en haut de fenêtre et non sous l'en-tête, parce qu'empiler deux bandeaux collants coûtait **40 % de la hauteur d'un téléphone** ; sur une fiche, `body.reading` rend donc l'en-tête normal (40 % → 23 %). Mesuré à la revue : `.player` gardait un `margin-bottom: 2rem` hérité du flux ordinaire — invisible quand ça défile, **32 px de chrome fixe** une fois collé (bloc 222 → 190 px sur mobile). **Revue de code appliquée avant fusion**, cinq correctifs : ~55 lignes de CSS `.sticky-player` orphelines (vestige d'une barre du bas abandonnée en cours de route) supprimées ; le hook `useHeaderHeight` publiait `--header-h` que plus aucune règle ne lisait — supprimé, commentaires corrigés ; le bloc « Chapitres » était rendu sans garde, donc un sermon sans chapitres offrait un dépliant qui n'ouvre rien (0/131 publiés, mais **8 lignes enrichies** sont déjà dans ce cas) ; le bloc « Thèmes » se fermait sur `topics_canonical`, faisant disparaître **aussi** les thèmes libres qu'il affiche encore — latent aujourd'hui, réel dès que l'enrichissement reprend ; enfin 260 Ko de captures commitées dans un répertoire nommé `$S` (variable shell non substituée) retirées du suivi. Vérifié : 153 tests verts, 0 erreur JS, aucun débordement de 320 à 1 280 px.

**M6i recherche plein-texte dans les transcriptions — DONE (2026-08-04, décision #59).** La recherche ne voyait que le contenu éditorial ; ce qu'un prédicateur a réellement DIT restait hors d'atteinte. Mesuré sur les 131 sermons publiés : **77 % du vocabulaire des transcriptions (16 586 mots sur 21 637) n'apparaît nulle part dans les champs éditoriaux** — et ce ne sont pas des mots sans intérêt : « cancer » (5 sermons), « divorce » (3), « hôpital » (7), « adolescence » (7), « propitiation » (2), « indulgences » (3). Exactement ce qu'un membre cherche, exactement ce qu'aucun résumé ne mentionne. **Architecture** (`scripts/build-fulltext.mjs` + `src/lib/fulltext.js`) : index inversé construit au build, **découpé par préfixe de terme**, donc une requête ne télécharge que les shards de ses propres mots. Mesuré : 21 637 termes, 338 shards, **médiane 0,6 Ko gzip, le plus gros 22 Ko** — là où un index monolithique ferait 244 Ko aujourd'hui et ~1 Mo à 517 sermons, à payer d'un bloc. Le redécoupage à 3 lettres se déclenche tout seul quand un voisinage grossit, donc **le coût d'une requête ne bouge pas quand le catalogue quadruplera**. **Le résultat n'est pas une page, c'est un INSTANT** : chaque sermon trouvé porte ses horodatages, un clic ouvre la fiche au bon moment avec la transcription déjà filtrée sur le mot (`?t=…&q=…`) — les timestamps capturés depuis #42 répondent enfin à la question inverse (« où, dans 131 sermons, a-t-on parlé de ça ? »). **Vérification par vérité terrain** : l'index a été comparé à un grep sur les VTT eux-mêmes, terme par terme — *galates* 34 sermons, *circoncision* 18, *espérance* 40, *abraham* 37, comptes d'occurrences identiques, aucun écart. Ce contrôle a révélé le défaut qui comptait : « prière » (76/131) et « pardon » (94/131) sont **écartés au build comme ubiquitaires** — pour une église, ce sont ses mots vides à elle — et une requête « prière du matin » butait alors sur son premier mot et ne rendait plus rien. Corrigé en NOMMANT les 527 termes écartés dans le manifeste : un mot ubiquitaire ne contraint plus la recherche, et l'interface le dit (« revient dans plus de la moitié des sermons — trop courant pour distinguer un sermon d'un autre ») au lieu de renvoyer un silence. Tokenisation et format d'encodage sont **partagés entre l'indexeur et le lecteur** (`src/lib/tokenize.js`, `src/lib/ftcodec.js`) : deux implémentations d'un même format divergent toujours, et l'écart serait muet. Piège corrigé au passage : l'apostrophe est un **séparateur** (« l'Église » → `eglise`), sinon le mot n'était trouvable qu'en tapant l'élision. **13 tests JS** ajoutés (`node --test`, sans dépendance, dans l'esprit stdlib du pipeline).

**M6j raison d'être du POC formalisée — DONE (2026-08-04, décision #58).** `apps/web-poc/README.md` : le POC n'est ni le premier livrable public ni un brouillon jetable, mais un **terrain d'essai permanent** dont le design et les fonctionnalités *sont* le produit. Voir #58.

## 8. Resolved design decisions (grilled 2026-06-13)
- **Scripture:** canonical **OSIS** book IDs (e.g. `Rom.8.1-8.4`) + a FR/EN parser; display localized, query canonical. Powers browse-by-book.
- **EN/FR pairing:** **independent** Sermon records linked by `translation_of` (each keeps its own media/transcript/thumbnail).
- **Versioning/audit:** **git history of the flat-file export** is the audit trail; live store is last-writer-wins. Zero extra infra.
- **Search:** **prebuilt static index** (FlexSearch/lunr/Pagefind) regenerated on export; near-zero ops, no server. Upgrade to a managed search server later only if scale demands.
- **Topic labels:** translated **FR + EN + PT** (bounded ~30–60 list); matches the trilingual UI.
- **YT↔SC matching (backfill):** **fuzzy title + date window**; high-confidence auto-link, low-confidence to the completeness dashboard; orphans (EN-only videos) flagged, not forced.

### Decision (added)
- **#38 (2026-06-13) — Storage split:** **transcripts are committed to git** (`data/catalog/transcripts/*.txt`, ~25 MB total — text, canonical, diffable). **Audio is never committed** — downloaded to a gitignored `cache/`, deleted after transcription; it's regenerable via `yt-dlp` and the church's SoundCloud/YouTube are the durable audio archive. Media artifacts (thumbnails) later go to object storage / WP media, referenced by URL — not git.
- **#39 (2026-06-13) — Pipeline shape:** single entry point `build_entry(source, *, transcribe, enrich)` composing parse → transcribe → enrich → assemble; the two external steps are **injected** (real = mlx-whisper + Claude API; tests inject fakes) → fast offline deterministic tests + one global integration test. Production enricher = Claude `claude-sonnet-4-6`, structured JSON output (needs `ANTHROPIC_API_KEY`). *(Cost & model in #40; now ~$0.078/sermon with the expanded schema — M5l.)*
- **#40 (2026-06-14) — Enrichment cost (measured) + model choice:** The real POC run cost **$0.51 for 8 full sermons ⇒ ~$0.06/sermon on `claude-sonnet-4-6`** — *3× the "~$0.02" figure carried in earlier docs, which was actually Haiku 4.5 pricing, never reconciled against a Sonnet run. Corrected throughout.* A **Haiku 4.5 vs Sonnet 4.6 bake-off on one rich sermon** (incarnation/Christology, `sc-2338530545`): both produced excellent, faithful, well-cited output (~identical scripture refs); **Sonnet wins on two material points for FR/theological content** — it names heresies precisely (docétisme, nestorianisme, modalisme) where Haiku stays generic, and its French is clean where Haiku slipped anglicisms/typos ("essential", "necessaires") and a malformed series label. Measured per-sermon: **Haiku $0.018, Sonnet $0.053. Decision: keep Sonnet 4.6 as the default enricher** — at ~$12 for the 239 SC sermons / **~$23 for the full 467**, the quality margin (precision + clean French an elder will read) is worth ⅓-extra. Haiku stays a documented fallback for bulk/budget runs. Full-pass budget figures corrected in `CLAUDE.md`. **Full bake-off table, sample outputs, and the Claude pricing reference: [`docs/ENRICHMENT-MODEL.md`](ENRICHMENT-MODEL.md).** *(Update — M5l 2026-06-16: the lean schema measured here was later expanded to the full WP set → **~$0.078/sermon**, ~$19/239 SC, ~$40/517. These $0.06/$12/$23 figures are the original lean-schema measurement.)*
- **#42 (2026-06-14) — Capture timestamps in the ASR pass (do it once):** The transcription step now emits **segment + word-level timestamps**, not just plain text — `transcribe_fn` returns `{text, vtt, segments, language}` and `build_entry` persists **`<id>.txt`** (plain, drives enrichment + search) + **`<id>.vtt`** (subtitle-ready segment timestamps) by default; the heavy **`<id>.json`** (word-level timing + per-segment confidence) is **opt-in** via `persist_segments=True` / the CLI `--segments` flag (see git-weight below). *Rationale:* ASR is the expensive step (download + ~15× real-time compute); emitting timestamps from that same pass via mlx-whisper `--output-format all --word-timestamps True` costs **nothing extra**, but re-deriving them later means re-running ASR on all 467. Capturing the maximum once unlocks, for free: **EN/PT subtitle generation** (translate the VTT cues, timing preserved), **audio-synced reading + deep-linking** ("jump to where he says X"), **search-to-moment**, and **confidence-flagged human review** (shaky stretches surface themselves). Conservative `clean_transcript` is applied to both text and VTT (its word-substitution rules never touch timestamp lines). Supersedes the text-only assumption in #38 — committed transcripts are now `.txt` + `.vtt`. **Git-weight (measured, decision validated 2026-06-14):** for a 59-min sermon, `.txt` 48 KB · `.vtt` 117 KB (~2.4×) · `.json` **1.5 MB (~30× the txt, ~220 KB gzipped)**. Word-level JSON is therefore **not committed** — kept opt-in (`--segments`) and regenerable from audio (ASR is local/free). Decision: **`.txt` + `.vtt` only for now** — segment-level timing covers subtitles/deep-linking, and even Grace-to-You doesn't ship word-level sync; revisit if a concrete use case (karaoke highlight, sub-second search-to-moment) justifies the weight.
  **Naming convention:** the filename is a *primary key, not a label* — **`<entry-id>[.<lang>].<ext>`**, flat in `data/catalog/transcripts/`. `<entry-id>` is the catalog `id` (`sc-<soundcloud_id>` / `yt-<youtube_id>`, from `build_entry`); all formats for one sermon share the stem (`sc-X.txt`/`.vtt`/`.json`), and the stem *is* the foreign key into `catalog.json`. Ids are stable; titles/dates/slugs drift and already live in the catalog, so they're kept out of filenames. Translated subtitles add a language infix (`sc-X.en.vtt`). *(Contrast: `docs/research/spike-transcripts/` keeps the spike's readable `NN_date_slug` names — right for a frozen 8-file evidence folder a human browses, wrong for a 467-row store tooling joins. Different masters, deliberately not unified.)*
- **#41 (2026-06-14) — ASR backend is pluggable (mlx-whisper now, cloud Whisper API in production):** Transcription is isolated behind `make_transcriber()` in `transcribe.py` and **injected** into `build_entry` (#39), so the ASR engine is a swappable adapter. **Now:** mlx-whisper `large-v3-turbo`, local on Apple Silicon, free, ~15× real-time — ideal for the one-time backfill on a Mac. **Production / new-sermon flow:** the church's machines are **Windows laptops** where mlx (Apple-only) won't run, so route to a **cloud Whisper-class API** (e.g. Groq `whisper-large-v3`, OpenAI, or a hosted endpoint) behind the same injected `transcribe_fn(source) -> {text, vtt, segments, language}` contract (see #42) — only `transcribe.py` changes, nothing downstream. The committed transcripts (#38) mean re-transcription is rarely needed; the backend choice is an operational detail, not a data-model one.
- **#43 (2026-06-14) — YT↔SC matcher produces false positives at the duration-collision boundary (found in the wild):** Validating the pipeline on YouTube video `IqNmh_XGULE` ("Prier selon le cœur de Dieu IV | **Psaume 40**") surfaced that it had been auto-matched as the `youtube_id` of an *unrelated* SoundCloud sermon `sc-2054926060` ("Image de Dieu | **Genèse 1:26-2:3**") at `youtube_match: 0.656`. Cause: the duration fingerprint — YT 3555 s vs SC 3550 s, a **5-second collision between two different ~59-min sermons** — plus a weak title signal cleared the auto-accept bar. This is the concrete instance of the "dedup edge case" risk. **Audit (2026-06-14):** of the 60 accepted matches, the ≥0.7 band (24) was clean but the **0.6–0.7 band (36) was duration-only** — 35/36 had near-zero title overlap, and all 3 that were scripture-checkable were false (incl. *Psaume 40 Parties IV **and** V* scattered onto unrelated Genèse/Colossiens sermons). **Fix (durable, in `match_youtube.py`):** the duration fingerprint now only *corroborates* — a match is accepted only when title agreement (`TITLE_STRONG = 0.62`) or exact scripture carries it; duration alone never does (`TITLE_WITH_DUR = 0.45`, `TITLE_WITH_SCR = 0.25`). Rebuilt via `build.py`: **same-language overlaps 61 → 18, translations 11 → 4, orphans 228 → 278, union 467 → 517** — ~43 "matches" were duration coincidences, and those YouTube videos now stand as their own entries. **Verified:** 0 book-mismatch false positives remain, and 0 orphans have a strong (≥0.6) title match to an SC sermon (nothing real was wrongly demoted). Supersedes the earlier one-off hand-patch (M5g, catalog 468).
- **#44 (2026-06-14) — Enrichment must be a separate layer, not stored in `catalog.json`:** `build.py` regenerates `catalog.json` from raw (parse→match→fold→cluster), so any enrichment written directly into it (topics/summary/`transcript_ref`/`scripture_refs`) is **wiped on the next rebuild** — observed when the #43 matcher rebuild erased the hand-added `yt-IqNmh_XGULE` enrichment (transcripts on disk survived; the catalog row went back to `topics:[]`). Therefore the full enrichment pass must write to a **separate id-keyed store** (e.g. `data/catalog/enrichment.json`) that a **writeback step merges into the rebuilt structural catalog**. Build this writeback *before* the catalog-wide enrichment pass, or every structural rebuild destroys ~$25 of work. (Transcripts under `data/catalog/transcripts/` are safe — they're keyed by id and not touched by `build.py`.) **RESOLVED (2026-06-16):** `pipeline/enrichment_store.py` implements the store (`data/catalog/enrichment.json`, id-keyed) + `writeback()`, now the final step of `build.py` (parse→match→fold→cluster→**writeback**). `save_entry(build_entry_result, model=…)` upserts per sermon during the pass; `apply()` is a pure merge of the enrichment fields (`topics, summary, primary_scripture, scripture_refs, series_hint, transcript_ref, captions_ref, segments_ref`) onto rows by id, leaving structure untouched. *(`speaker` is deliberately NOT stored — it's structural, set by the default-speaker rule #45 on each build.)* Seeded with the 9 entries enriched so far (8 samples + `yt-IqNmh_XGULE`); verified by a full `build.py` rebuild from raw — the 9 rows came back enriched (517 rows, 9 enriched). 3 offline tests cover the merge.
- **#37 (2026-06-13):** The catalog is the **UNION of SoundCloud + YouTube** (**467** distinct sermons), not the SC spine alone. SC = clean French expository audio (239); YouTube adds conference content (~45) + English versions + SC-absent French sermons. YT↔SC are largely *complementary*: with full duration coverage (239/239), duration-fingerprint matching (YT ≈ SC + 51 s) confirms **61** same-language overlaps + **11** translations; the remaining **228** YT videos are net-new.

- **#45 (2026-06-16) — Default-speaker rule + voice-ID deferred:** the regular, lusophone-accented preacher is **Pastor David Pelosi**; untagged sermons default to him. `resolve_speaker()` (scripture.py): an explicit speaker named in the title wins; **conference/English sermons are left unattributed** (typically named guests — don't mis-credit the pastor); everything else → David Pelosi. Applied in `parse_catalog`, `fold_orphans`, `build_entry`; **speaker is structural** (re-derived each build, not in the enrichment store). Rebuilt: attribution **45 → 473/517** (David Pelosi 449 + explicit guests like Stephan Kongo, Joël Beeke). *Caveat:* an untagged **guest** sermon gets mis-credited to the pastor — provenance is "default rule", correctable opportunistically. **Voice identification (diarization / speaker embeddings) considered and DEFERRED:** it needs heavy extra ML (pyannote/PyTorch + a voice-enrollment sample), runs on audio (re-downloadable, ~1–2× real-time), and the title default already covers the dominant case for free — revisit only if mis-attribution becomes a real problem, or for the bilingual EN-conference + live-translator case where two voices share one recording. **The goal is a binary gate, not 1:N identification:** "is this David?" → keep the default; "clearly not David" (rare) → leave blank for manual fill. **Now kept by default to support this:** the **uncleaned** ASR transcript as `<id>.raw.txt` (build_entry persists it; `raw_ref` in the store) — David's lusophone accent leaves systematic ASR artifacts there (the ones `clean_transcript` strips), so the raw text *vs* cleaned diff is itself an accent signal. (mlx-whisper gives words + timestamps + per-segment confidence, **not** voice/tone — a true voiceprint needs a separate speaker-embedding model on the audio; see the build-log assessment.)

- **#46 (2026-06-16, PLANNED) — "Sermon fingerprint" store for speaker attribution (curated, human-named, lightweight):** ~5 regular preachers (David + elders Stephan, Nathanaël, Loïc, Christian) cover 90%+; the rest are guests/trainees. Goal: a per-sermon **fingerprint**, recurring fingerprints → centroids a human **names**, new sermons snap to the nearest centroid → suggested speaker (or "unknown → leave blank"). At 500 sermons / 5 speakers this is **nearest-centroid classification** — no training, no neural nets, no infra.
  - **Store:** `data/catalog/fingerprints.json`, id-keyed: `{ features|embedding, assigned_speaker, method, confidence }`. Same id-keyed + writeback pattern as the enrichment store (#44); writeback sets `speaker` + **provenance ladder** `title > fingerprint-confirmed > fingerprint-suggested > default-rule (#45)` — never silently overwrite a human label.
  - **Two substrates.** *Tier A — text* (`pipeline/fingerprint.py`, pure stdlib): function-word stylometry (Burrows Δ) + **accent-artifact rate** (from the raw transcript — `clean_transcript` match count per 1k words; David's lusophone accent spikes it) + cadence (words/sec from the VTT) + vocab richness. Free, nails *David vs not-David*; **open question whether it separates the native-French elders from each other.** *Tier B — audio* (injected adapter, one small dep e.g. Resemblyzer): speaker-embedding verification, embed each sermon during the pass (audio already downloaded), cosine to per-speaker centroid. Reliable 5-way; **not** diarization (we have single-speaker recordings).
  - **Workflow (human-in-the-loop):** seed centroids from title-labeled sermons (ground truth) → classify the rest → a human confirms/names in batches → store + writeback. Bias the threshold toward *keeping the default* (only blank on confident not-David), since not-David is rare.
  - **Bonus:** the same fingerprints flag duplicates/re-uploads and can corroborate the YT↔SC matcher (#43).
  - **Test (M5k, DONE 2026-06-16):** transcribed 20 labeled sermons (David ×8, Stephan ×6, Nathanaël ×2, Loïc ×1, Christian ×1, Beeke ×1, Jonas ×1) and ran Tier-A leave-one-out. **Result: 12/16 — Stephan 6/6 ✓, David 6/8, Nathanaël 0/2 ✗.** Two clear findings: (1) **text stylometry works only with enough examples** — Stephan (6) separated perfectly, but the thin-data elder Nathanaël (2) failed entirely; the elders mostly have 1–2 labeled sermons, so centroids are starved. (2) **The accent-artifact feature is effectively dead** (`artifact/1k`≈0): `count_artifacts`'s 7 curated phrases are far too sparse to fingerprint an accent — the hypothesized cheap David signal did not materialize. **Conclusion: Tier A is a weak *review-flag* (~75–85%), not reliable elder attribution.** For a real "name the 5 preachers" store, **Tier B (audio embeddings) is the recommended substrate** — one enrollment clip per voice sidesteps the data-starvation that cripples text centroids, and voices separate where same-church text styles don't. Decision: keep Tier A only as an optional "looks-not-David → review" flag; build the fingerprint store on audio embeddings if/when speaker attribution is prioritized.

- **#47 (2026-06-17) — 30-sermon checkpoint findings (two text-layer issues, neither blocks the 500):** The hand-picked 30-sermon batch (M5n; store 9→39, ~7.5%) confirmed cost (**$0.0705/sermon ⇒ ~$36/517**) and surfaced two quality issues — **both re-runnable from the committed transcript+VTT (no re-download, no re-ASR), so they are NOT one-shot and do not gate the catalog-wide pass.** (a) **English sermons lose timestamp-anchoring + verbatim extraction:** enrichment is emitted in French (correct — French audience) but the VTT is English, so `_vtt_locate` can't match the French chapter/quote anchors → EN chapters return `t:None`, and key_quotes/scripture_refs come back empty (28 EN records). Fix options when prioritized: anchor quotes/chapters in the *source* language then translate the display text, or accept EN sermons ship without deep-links. (b) **Short clips fabricate structure:** the schema fills to the requested counts regardless of input length (a 2-min clip produced 6 chapters / 6 key_points) — argues for scaling extraction targets to duration. **Operational finding (not a decision, logged for the runbook):** the **YouTube download path needs retry/back-off** — intermittent `HTTP 403` throttling hit 3 of 12 YT videos, all cleared on a simple retry; the SoundCloud spine had zero failures. Resumability (#45) absorbed the harness reaping long background runs with zero wasted compute.

- **#48 (2026-06-17) — Language is decided by the AUDIO, not the title (+ retry on garbage):** Investigating #47(a) revealed the EN problem was deeper than anchoring. **`asr()` hard-coded `--language fr`**, so a genuinely-non-French sermon transcribed to pure `...` and its "French" enrichment was *hallucinated from the title* (empty transcript). And the catalog `language` was title-derived → unreliable: 2 of 3 "English" sermons in the batch were **French audio with English titles**; 1 French sermon (`yt-HOjW63LMfhQ`) only transcribes under `--language en` (a non-French intro makes the fr decoder collapse). Whisper's own 30-second auto-detect is also unreliable both ways (caught a French sermon opening with "Thank you" as English). **Resolution:** (1) default to forced French (proven on the 489 SC sermons — no regression to the bulk); (2) if the result *looks garbage* (real-char ratio < 0.3), **retry forcing English**; (3) set the catalog `language` from the **transcript content** (FR/EN function-word ratio over thousands of words, `detect_language()`), never the 30s detector; (4) `language` moved into the enrichment store so the audio-derived value overrides the title guess across rebuilds. Also fixed: **`asr()` now clears its output sidecars before each run** — mlx-whisper skips regenerating when output exists, so the fr→en retry was silently reading back the stale fr garbage. And the **enrichment prompt** now keeps `anchor`/`key_quotes` in the transcript's own language (don't translate) so they remain locatable. Net: language is now audio-truthful + EN/mixed sermons transcribe correctly. Re-runnable from audio; only `yt-HOjW63LMfhQ` (the one genuinely-broken transcript) needed re-ASR. Tests 56 → 63.
- **#49 (2026-06-17) — Audio speaker attribution wired (the deferred half of #46):** Closed the loop the voiceprint *capture* (#46) left open — until now `speaker` came only from the title default-rule (#45), so the CSV showed just `title`/`default-rule` and known mislabels stood. `pipeline/speaker_id.py` builds one **centroid per speaker from ground-truth voiceprints only** (title-confirmed + human overrides — never the unverified default-rule, which would poison David's centroid with the very mislabels we're hunting), classifies every sermon's voiceprint by **cosine** to the nearest centroid, and applies via the ladder **human > title > audio-fingerprint > default-rule** (audio fixes default-rule/blank guesses; it **flags** rather than overrides an explicit title). **Leave-one-out 10/10** on speakers with ≥2 examples; calibration is clean — same-speaker cosine ≥ 0.94, cross-speaker ≤ 0.89 — so the accept floor sits at **0.90** in that gap, with single-example centroids held back to "review". **Result: 15 catalog rows upgraded to `audio-fingerprint`** — the audio independently caught the Ézéchiel/Leçon sermons as **Stephan** (sim 0.95-0.97, the known mislabels) and verified default-rule David guesses. **Key limitation surfaced:** David's centroid has only 2 confirmed examples, so many genuine David sermons score 0.85-0.87 (below the floor) → human-confirm a few more David sermons (`speaker_overrides.json`) to sharpen it. Re-runnable from the stored vectors; its own store (`speakers.json`) + writeback. Tests 63 → 73.

- **#50 (2026-06-17) — Series-prior ground truth for speaker attribution (domain knowledge → voiceprint-verified):** Title-named ground truth is exhausted for the elders (Loïc/Christian have 1 sermon each in the *entire* inventory; nothing more to download — scanned all three raw inventories incl. the 102 unfolded Live services). Two church-domain priors break the data-starvation instead: **David preached Galatians for 3+ years** (a 71-sermon David pool), and **an elder takes a whole book when David is away** (Stephan → Ézéchiel, Nathanaël → Jacques). Encoded as `SERIES_SPEAKER` in `speaker_id.py`, used as ground truth **but verified by within-series cohesion** — each series builds its own voiceprint centroid; members below cosine 0.88 are flagged as outliers (a guest week), never blindly trusted. Cheap **voiceprint-only capture** (`capture_voiceprints.py`: download → embed → delete, NO ASR, NO API cost) filled the data: 76 title voiceprints + 41 series (20 Galates, 11 Ézéchiel, 14 Jacques) → **117 total**. **Result: the priors held empirically** — Galates 20/21 cohere (1 borderline-noisy David recording flagged), **Ézéchiel 11/11**, **Jacques 14/14**. Centroids: David **2 → 40**, Stephan **6 → 19**, Nathanaël **2 → 16**. Re-run attribution: **55 rows now `audio-fingerprint`** (David 30, Stephan 13, Nathanaël 12); in the 117-sermon sample **25 are audio-confirmed not-David** (Stephan owns Ézéchiel, Nathanaël owns Jacques). The thin single-example guests (Loïc/Jonas/Christian) stay "review" — still no more of them to capture. Tests 73 → 79.

- **#51 (2026-06-17) — Title-invisible elders found by series + regular/guest split:** Loïc and Christian are core elders who preach often but are almost never named in titles (1 title sermon each) — so the title method can't see them. Resolved the same way as #50, in reverse: find the *ownerless* series. The biggest unattributed series, **Épître aux Hébreux (29)**, was the Loïc hypothesis (he preached the most of the elders); **Évangile de Jean (5)** the Christian hypothesis (his one title sermon sits there). Voiceprint-verified: **Hébreux 25/29 cohere, series-centroid nearest Loïc 0.953** (vs David 0.880); **Jean 4/5 cohere, nearest Christian 0.960** (vs David 0.915). Both added to `SERIES_SPEAKER`. Centroids: **Loïc 1 → 26, Christian 1 → 4**. Also added the **regular/guest split**: a non-conference sermon can only be one of the 5 regular preachers (David + elders), so a thin conference-guest centroid (Jonas Hensworth, Borgman, …) no longer attracts church sermons. **Now: 80 rows `audio-fingerprint`** (David 32, Loïc 20, Stephan 13, Nathanaël 12, Christian 3); of the 147-voiceprint sample, **48 are audio-confirmed not-David**, each elder owning their book (Loïc→Hébreux, Stephan→Ézéchiel, Nathanaël→Jacques, Christian→Jean). General capability reached: any sermon, titled or not, attributes to a preacher from voice alone. Tests 79.

- **#52 (2026-06-17) — Closed-set decision for the 5 distinct voices; `series_name` ≠ preacher series:** Church feedback — preachers never take over each other's series, and the 5 sound nothing alike — prompted two corrections. (1) **`series_name` is auto-clustered by Bible BOOK, not by preacher**, so a book-group can sweep in a regular's one-off or a guest's conference talk (the Hébreux cluster held Loïc's 26-sermon series + **2 David sermons, both on Hébreux 1** = David opened the book, Loïc preached the rest + a mis-clustered Brian Borgman CBN talk). The voiceprint correctly separates these — the cohesion "outliers" were never preacher-takeovers, they're clustering artifacts. (2) **A leave-one-out among the 5 regular preachers is 99% (104/105)** — they ARE acoustically distinct, so the apparent earlier "confusion" was guest-centroid noise (fixed by the #51 regular/guest split) + an over-strict absolute floor. For the *closed set* of regular sermons the reliable signal is "nearest regular, won by a clear margin", not high absolute similarity (which recording/era variation alone depresses); floor lowered to 0.80 + margin 0.03 for non-conference (conference keeps 0.90 open-set). Verified a mid-sermon-window embedding ≈ full-audio (the low scores are recording variation, not non-sermon audio dilution). **Result: 100 rows `audio-fingerprint`** (David 43, Loïc 27, Nathanaël 14, Stephan 13, Christian 3); 57 of the 147-voiceprint sample are not-David. Tests 79.

- **#53 (2026-06-17) — Frozen canonical record contract + Sonnet-only:** The catalog row shape lived implicitly in two hand-maintained lists (`fold_orphans.CANON` structural + `enrichment_store.ENRICHMENT_FIELDS`). `pipeline/schema.py` makes it the explicit frozen contract (40 fields, types, required vs optional), validates every row, and emits a formal JSON Schema (`data/catalog/sermon.schema.json`) for the WordPress import + external consumers. A **drift guard** (tested) asserts `CANON ∪ ENRICHMENT_FIELDS == schema.FIELDS`, so the contract can't silently diverge from what the code writes; `build.py` validates as its final step. The freeze immediately caught a real inconsistency — `youtube_match` was a bare match-score (number), not the object it was assumed to be. **Model decision: Sonnet-only for the full pass.** Haiku 4.5 was reconsidered as a budget path for the YT-only bulk and rejected — its French/theological precision was materially worse (the #40 bake-off finding holds in practice); the ~3× cost of Sonnet buys output an elder will actually read. Full 517 ≈ **~$36 on `claude-sonnet-4-6`**. Tests 79 → 88.

- **#54 (2026-07-30) — The catalog gets a demo consumption window before the WordPress library:** `apps/web-poc/` (React 18 + Vite 6) renders the catalog as a browsable sermon library — the first time the dataset is *consumed* rather than built. Its stated purpose (plan: [`docs/plans/WEB-POC.md`](plans/WEB-POC.md), status: [`WEB-POC-STATUS.md`](plans/WEB-POC-STATUS.md)) is **to show the elders what the pipeline produces and to de-risk the future WordPress library** — it does **not** displace the roadmap's M5 WP sermon section, and `sermon.schema.json` (#53) stands as the WP-import contract. The load-bearing technical choice is **build-time projection, not an API**: `scripts/build-data.mjs` reads `data/catalog/catalog.json` + `transcripts/*.vtt` and emits static JSON/VTT into `public/data/` (gitignored, a build artifact) before every `dev`/`build`, so the app has **no runtime coupling to the pipeline and no server** — consistent with the static-index decision (§8) and with "apps are replaceable windows". The gate is `description && summary`, so coverage grows by itself as enrichment advances: **131 of the 139 enriched rows publish today** — the 8 M5b sermons were enriched under the earlier lean schema (no `description`) and need a ~$0.55 re-enrich from their committed transcripts to appear. **Still WIP:** the sermon page (player seek, synced transcript) is verified only in the data layer and on the home screen, not on screen end-to-end; the YouTube IFrame path has never run. **DEPLOYED (2026-08-01): https://kevymbappe.github.io/sermotheque-ebn/** — the repo was made public (#55), so GitHub Pages is free; the "paid plan / mirror `dist/` to a public repo" fallback is moot. `.github/workflows/deploy-poc.yml` builds and publishes on pushes to `main` touching `apps/web-poc/**` **or `data/catalog/**`** — so an enrichment pass republishes the site with no code change, which is the projection rule (#54) carried into CI. **One manual prerequisite, learned the hard way (2026-08-01):** Pages must be switched on once by a human — Settings → Pages → Source = *GitHub Actions*. `actions/configure-pages`' `enablement: true` was tried and **cannot work**: creating the Pages site needs repo-admin rights, which a workflow's `GITHUB_TOKEN` does not have (`Resource not accessible by integration`). Everything else in the run succeeded on the first try — install, catalog projection (131 sermons), Vite build.

- **#55 (2026-08-01) — The repository is public:** visibility flipped from private to public (verified via the GitHub API: `"private": false`). **Consequence taken deliberately:** the whole canonical dataset is now world-readable — the 139 full transcripts, the inventories, the speaker attributions (including the `default-rule` guesses, which are *inferences*, not confirmations), and the pipeline. That is consistent with the content itself: every sermon is already published on the church's public YouTube and SoundCloud. **What it unblocks:** GitHub Pages is free on public repos, which removes the only blocker on the #54 deploy. **What to watch:** `.env` stays gitignored (no key has ever been committed — `.env.example` is the only template in git), and any future personal data (contact details, unpublished recordings, member names beyond the preachers already named in public titles) must not enter `data/`. If the church later wants the archive non-public, the repo can go private again — but published git history cannot be un-published.

- **#56 (2026-08-01) — In-body scripture citations get a normalised twin, derived not stored:** `scripture_refs` (the passages quoted *inside* a sermon, extracted by the LLM) is free French text — `"Jean 1:1,14"`, `"Genèse 9-10"`, `"Luc 15 (fils prodigue)"`. Good for a human, useless for an index. Rather than change its shape (the web POC renders it as text, and rewriting an LLM output in place would lose the original wording), a **second field `scripture_refs_osis`** carries the same citations as canonical OSIS ids. Three choices worth recording. **(1) Additive, not destructive:** `scripture_refs` stays the display half, `scripture_refs_osis` is the queryable half — same pattern as `scripture_display` / `scripture_osis` on the structural side. **(2) Derived, never persisted:** it is computed by the enrichment writeback from `enrichment.json` and deliberately excluded from `extract()`, so it is **recomputed on every build**. A stored copy could outlive the parser that produced it; as written, improving the normaliser improves all 517 rows with **zero API calls** — which is exactly the re-runnability that #52 is built on. `enrichment_store.DERIVED_FIELDS` names the category so a future derived field follows the same rule, and the #53 drift guard still holds (`CANON ∪ ENRICHMENT_FIELDS == schema.FIELDS`, now 41 fields). **(3) A separate parser from `parse_scripture`, sharing the book table:** the title parser is tuned for one reference embedded in noise and the structural catalog depends on its exact output, so it was left untouched. Citation strings need different rules — most sharply, **two numbers with a dash mean chapters when no verse separator is present** (`Genèse 9-10` = chapters 9–10; the title parser would read `Gen.9.10`). `parse_reference` reuses `BOOKS`/`fold` and adds: verse lists → several ids, cross-chapter spans, dot separators, French `ch.`/`v.`/`à`, single-chapter books (`Jude 24-25` → `Jude.1.24-Jude.1.25`), several books in one string, parenthetical glosses stripped unless the parenthesis is itself a citation, and word-boundary guards so `Jean-Baptiste` is not the gospel of John. **Never guess:** an unrecognised book yields `[]` and is counted. Measured on the committed data: **1 243/1 243 citations parsed, 0 failures, no number silently dropped**; **54 distinct books reachable via body citations vs 22 via `scripture_book` on the same 139 rows**. Tests 88 → 123. **Deliberately out of scope:** the inverted index itself (`Rom.8` → sermons) — this decision only makes it possible.
  **⚠️ Trap found while writing this, worth more than the feature: never put a literal accented character inside a regex or a `str.replace` in this codebase.** An `à` typed into a pattern was stored as **NFD** (`a` + U+0300 combining grave) rather than NFC, so the substitution silently never fired — no error, just a rule that quietly does nothing. `parse_reference` therefore contains no literal accent at all: `à` is handled *after* `fold()`, as a bare `a`. **The same latent bug sits in `parse_scripture` today** (`ref.replace("à", "-")` runs on already-folded text, where `à` no longer exists), together with a second one in the same area: a bare chapter range in a title (`Genèse 1-3`) is read as chapter 1 verse 3, not chapters 1–3. **Measured impact right now: 0 of the 517 rows** — no committed title uses a bare chapter range, which is why it has never shown up. Left unfixed on purpose: touching it would rewrite `scripture_osis` on the structural catalog, which deserves its own change with its own verification. Fix it before the next title-parsing work, not as a drive-by.

- **#57 (2026-08-01) — Topics get a curated vocabulary, derived not stored:** The enrichment writes free-text topic labels — rich and faithful to each sermon, but uncontrolled. Measured on the committed data: **1 086 labels for 594 distinct forms, 451 of them used exactly once**. « Herméneutique » sat beside « Herméneutique biblique », « Union avec Christ » beside « Union à Christ », « Image de Dieu » beside « Imago Dei » and « Image de Dieu (Imago Dei) ». A list like that has no floor: it cannot be a filter, and it cannot be navigated — which is why the web POC only ever used topics as free-text search fodder. `pipeline/topics.py` implements the vocabulary the original topic decision always called for (~30-60 curated terms, AI-bootstrapped, human-confirmed): **44 categories**, each with aliases *taken from the real data, not imagined*, plus `canonicalize()` and `normalize_topics()`. Same two principles as #56 — **additive** (`topics` stays the display half, `topics_canonical` is the queryable half; the LLM's wording is never rewritten) and **derived, never stored** (recomputed at every writeback, so extending the vocabulary improves the whole catalog at zero API cost). **Result: 98% of label occurrences classified** (1 072/1 086), 44/44 categories in use, ~5.9 per sermon. The **14 remaining occurrences are all book or passage names** — « Épître aux Hébreux », « Ézéchiel », « Tite 2 » — which are not topics at all and are excluded on purpose by a stoplist; the catalog already indexes them through scripture (#56). **Nothing is forced into a category to make the number look good**: an unrecognised label yields nothing and is counted, because a mis-filed topic is worse than a missing one — it lies silently. `data/catalog/topics.json` (written by `build.py`) carries the vocabulary, its per-category counts and the unmapped list, so a human can extend it without reading code. **Three matching bugs found and fixed while building it, each silent:** (1) plain substring matching filed « soumission » under *mission* and « empêchements » under *péché* → alias matching is now word-boundary anchored; (2) Unicode decomposition does **not** touch the « œ » ligature, so « Pacte des œuvres » never met its alias → ligatures are unfolded explicitly; (3) a stoplist entry meant for « Évangile de Luc » was swallowing « Évangile de la grâce » → the book patterns were narrowed. Tests 123 → 153, most of them locking exactly these traps.

### Still open (lighter — resolve during build)
- **Stray Xcode project at the repo root** (`Sermothèque EBN/`, 12 files) — an untouched default SwiftUI/SwiftData template (`ContentView.swift`, `Item.swift`, `Sermothe_que_EBNApp.swift`). It contains no project code, and a native iOS target **contradicts PRD decision #9 (Expo/React Native, Android TV + Fire TV first)**. Either delete it or record a decision superseding #9 — it should not sit in the tree unexplained.
- **Unfinished capture pass (M5u)** — 139/517 transcripts, and 79 ids with a voiceprint but no committed transcript. Audit that gap before resuming, otherwise those 79 are re-downloaded and re-ASR'd needlessly.
- **YT↔SC true overlap** — dedup the ~215 French YT orphans against SC needs a stronger signal than titles: re-pull `duration`+`upload_date` (both platforms) and match on those, or use embeddings/audio fingerprint.
- **Canonical hosting** — where the git export repo + static serving live (church-owned, per [PRD.md](PRD.md) §8 ownership model). Static index ⇒ static hosting, very low ops.
- **Dedup edge cases** — multi-part sermons, re-uploads, same-day multiples.

- **#58 (2026-08-04) — Le POC web est un terrain d'essai permanent, pas un premier livrable ni un brouillon :** La question laissée ouverte par #54 (« le POC web ou l'import WordPress comme premier livrable public ? ») posait un faux choix, parce qu'elle supposait que les deux visent la même chose. Ils ne visent pas la même chose. **Le POC répond à « regarde ce qu'on peut faire avec ces données »** ; le site de l'église répond à « voici les sermons de l'église ». Trois conséquences, toutes assumées. **(1) Le design et les fonctionnalités ingénieuses sont le produit, pas un supplément.** Un site correct mais quelconque échouerait à sa mission tout en fonctionnant parfaitement : ce qui fait comprendre la valeur du catalogue, c'est la recherche qui mène à la minute exacte, le lien partageable sur un instant, la fiche de groupe qui s'imprime. C'est ce qui justifie d'y investir du soin — et ce qui rendrait absurde de le juger à l'aune de ce dont WordPress a besoin. **(2) Il ne sera pas jeté quand la bibliothèque WordPress existera.** Il la précède (une idée s'y prototype en quelques heures au lieu de plusieurs jours) et lui survit (il reste l'endroit où l'on essaie ce qui n'y est pas encore) : une feuille de route exécutable, où chaque fonctionnalité qui tient devient une spécification **démontrée** plutôt que décrite, et un lieu où confronter des idées aux anciens — jusqu'à de l'A/B — avant de les engager sur le site de l'église. **(3) Ce qu'on y essaie est borné** : présentation, navigation, découverte. La vérité des données se corrige **en amont**, dans le pipeline ; laisser le POC corriger une donnée en ferait une seconde source de vérité, exactement ce que l'architecture en trois couches existe pour empêcher. Ce qui rend tout cela sans risque, c'est la projection au build (#54) : **aucune donnée ne vit dans le POC**, il ne peut pas dériver du catalogue, et il est jetable sans rien perdre — c'est précisément ce qui autorise à y expérimenter sans prudence. Consigné dans `apps/web-poc/README.md`, qui est l'énoncé de référence.

- **#59 (2026-08-04) — L'index plein-texte est découpé par préfixe : ce n'est pas son poids qui compte, c'est le poids d'une requête.** Chercher dans les transcriptions se heurtait à une contrainte simple : 16 Mo pour 131 sermons (63 Mo une fois les 517 captés), sans serveur pour les interroger (§8 : index statique pré-calculé). Un index monolithique aurait pesé 244 Ko gzip aujourd'hui et ~1 Mo à 517 sermons, **à télécharger d'un bloc à la première recherche**. Le découpage par préfixe de terme change la nature du problème : une requête ne charge que les shards de ses propres mots — **médiane 0,6 Ko, le plus gros 22 Ko** sur 338 shards — et le redécoupage à 3 lettres se déclenche tout seul quand un voisinage grossit, donc le coût d'une requête ne bouge pas quand le catalogue quadruplera. Trois choix de contenu en découlent. **(1) L'index stocke des minutes, pas des phrases** : au plus deux horodatages par (terme, sermon). Le texte autour se lit sur la fiche, où le VTT est de toute façon chargé ; l'embarquer dans l'index l'aurait triplé pour un détail affiché ailleurs. **(2) Les termes ubiquitaires sont écartés MAIS NOMMÉS.** Un mot présent dans plus de la moitié du corpus ne discrimine rien — pour cette église, « prière » (76/131) et « pardon » (94/131) sont des mots vides. Les écarter suffit à alléger l'index ; les nommer dans le manifeste est ce qui empêche le bug : sans cette liste, « prière du matin » butait sur son premier mot et ne rendait **rien**, alors que le second mot, lui, était indexé. Un mot ubiquitaire ne contraint pas la recherche, exactement comme un mot vide — et l'interface le dit plutôt que de renvoyer un silence. **(3) Tokenisation et format sont partagés entre l'indexeur et le lecteur** (`tokenize.js`, `ftcodec.js`) : deux implémentations d'un même format divergent toujours, et l'écart est muet — l'index se décoderait « avec succès » sur de mauvais nombres. Vérifié par **vérité terrain** (index comparé à un grep sur les VTT, terme par terme : aucun écart) et verrouillé par 13 tests `node --test`.

